{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Project",
            "type": "shell",
            "command": "${command:cmake.build}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "使用 CMake 插件编译整个 Qt 项目（快捷键 Ctrl+Shift+B）"
        },
        {
            "label": "CMake: Run Project",
            "type": "shell",
            "command": "${command:cmake.launchTargetPath}",
            "dependsOn": "CMake: Build Project",
            "problemMatcher": [],
            "detail": "先自动编译项目，编译成功后直接运行生成的 Qt 程序"
        }
    ]
}